# frozen_string_literal: true

source 'https://rubygems.org'

gemspec

# Keep development tooling out of the published gem metadata and pin RuboCop to
# the Ruby-2.6-compatible release line exercised by local development and CI.
gem 'minitest', '~> 5.16'
gem 'rake', '~> 13.0'
gem 'rubocop', '~> 1.50.0'
